export const environment = {
  production: false,
  api: 'http://localhost:8000/api/v1',
  // When true the app will use local mock data instead of calling backend.
  // Toggle to false to enable real API calls.
  useMock: true,
};
