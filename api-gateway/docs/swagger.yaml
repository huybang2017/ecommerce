basePath: /api/v1
definitions:
  models.AddToCartRequest:
    properties:
      product_item_id:
        example: 1001
        type: integer
      quantity:
        example: 1
        type: integer
    type: object
  models.AddressInfo:
    properties:
      city:
        example: Ho Chi Minh City
        type: string
      country:
        example: Vietnam
        type: string
      id:
        example: 1
        type: integer
      is_default:
        example: true
        type: boolean
      postal_code:
        example: "70000"
        type: string
      state:
        example: Ho Chi Minh
        type: string
      street:
        example: 123 Main St
        type: string
      user_id:
        example: 1
        type: integer
    type: object
  models.CartItem:
    properties:
      image_url:
        example: https://cdn.example.com/images/1.jpg
        type: string
      is_selected:
        example: true
        type: boolean
      price:
        example: 15100
        type: number
      product_item_id:
        example: 1001
        type: integer
      product_name:
        example: Găng tay chơi game
        type: string
      quantity:
        example: 2
        type: integer
      shop_id:
        example: 10
        type: integer
      sku:
        example: GT-123
        type: string
      subtotal:
        example: 30200
        type: number
    type: object
  models.CartResponse:
    properties:
      items:
        items:
          $ref: '#/definitions/models.CartItem'
        type: array
      total_items:
        example: 2
        type: integer
      total_price:
        example: 30200
        type: number
      updated_at:
        example: "2026-01-15T12:00:00Z"
        type: string
      user_id:
        example: 1
        type: integer
    type: object
  models.CartValidationResponse:
    properties:
      errors:
        example:
        - '[''Product out of stock'''
        - '''Invalid item'']'
        items:
          type: string
        type: array
      message:
        example: Cart is valid for checkout
        type: string
      valid:
        description: Whether the cart is valid for checkout
        example: true
        type: boolean
    type: object
  models.Category:
    properties:
      children:
        items:
          $ref: '#/definitions/models.Category'
        type: array
      created_at:
        example: "2025-12-23T10:00:00Z"
        type: string
      description:
        example: Electronic devices and gadgets
        type: string
      id:
        example: 1
        type: integer
      name:
        example: Electronics
        type: string
      parent:
        $ref: '#/definitions/models.Category'
      parent_id:
        example: 0
        type: integer
      slug:
        example: electronics
        type: string
      updated_at:
        example: "2025-12-23T10:00:00Z"
        type: string
    type: object
  models.ChangePasswordRequest:
    properties:
      new_password:
        example: newpassword123
        minLength: 6
        type: string
      old_password:
        example: oldpassword123
        type: string
    required:
    - new_password
    - old_password
    type: object
  models.CreateAddressRequest:
    properties:
      city:
        example: Ho Chi Minh City
        type: string
      country:
        example: Vietnam
        type: string
      is_default:
        example: false
        type: boolean
      postal_code:
        example: "70000"
        type: string
      state:
        example: Ho Chi Minh
        type: string
      street:
        example: 123 Main St
        type: string
    required:
    - city
    - country
    - postal_code
    - state
    - street
    type: object
  models.CreateCategoryRequest:
    properties:
      description:
        example: Electronic devices and gadgets
        type: string
      name:
        example: Electronics
        type: string
      parent_id:
        example: 0
        type: integer
      slug:
        example: electronics
        type: string
    required:
    - name
    type: object
  models.CreateOrderRequest:
    properties:
      address_id:
        example: 11
        type: integer
      payment_method:
        example: cod
        type: string
    type: object
  models.CreateProductRequest:
    properties:
      category_id:
        example: 1
        type: integer
      description:
        example: Latest iPhone with A17 Pro chip
        type: string
      images:
        items:
          type: string
        type: array
      is_active:
        example: true
        type: boolean
      name:
        example: iPhone 15 Pro
        type: string
      price:
        example: 999.99
        type: number
      sku:
        example: IPH15P-001
        type: string
      status:
        example: ACTIVE
        type: string
      stock:
        example: 50
        minimum: 0
        type: integer
    required:
    - description
    - name
    - price
    - sku
    type: object
  models.ErrorResponse:
    properties:
      error:
        example: Bad request
        type: string
      message:
        example: Invalid input
        type: string
    type: object
  models.LoginRequest:
    properties:
      email:
        example: user@example.com
        type: string
      password:
        example: password123
        type: string
    required:
    - email
    - password
    type: object
  models.LoginResponse:
    properties:
      access_token:
        example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
        type: string
      message:
        example: login successful
        type: string
      user:
        $ref: '#/definitions/models.UserInfo'
    type: object
  models.OrderItem:
    properties:
      price:
        example: 15100
        type: number
      product_item_id:
        example: 1001
        type: integer
      product_name:
        example: Găng tay chơi game
        type: string
      quantity:
        example: 2
        type: integer
      subtotal:
        example: 30200
        type: number
    type: object
  models.OrderResponse:
    properties:
      created_at:
        example: "2026-01-15T12:05:00Z"
        type: string
      items:
        items:
          $ref: '#/definitions/models.OrderItem'
        type: array
      order_id:
        example: 5001
        type: integer
      order_number:
        example: ORD-20260115-5001
        type: string
      status:
        example: pending
        type: string
      total_amount:
        example: 30200
        type: number
    type: object
  models.Product:
    properties:
      category:
        $ref: '#/definitions/models.Category'
      category_id:
        example: 1
        type: integer
      created_at:
        example: "2025-12-23T10:00:00Z"
        type: string
      description:
        example: Latest iPhone with A17 Pro chip
        type: string
      id:
        example: 1
        type: integer
      images:
        items:
          type: string
        type: array
      is_active:
        example: true
        type: boolean
      name:
        example: iPhone 15 Pro
        type: string
      price:
        example: 999.99
        type: number
      sku:
        example: IPH15P-001
        type: string
      status:
        example: ACTIVE
        type: string
      stock:
        example: 50
        type: integer
      updated_at:
        example: "2025-12-23T10:00:00Z"
        type: string
    type: object
  models.ProductsResponse:
    properties:
      limit:
        example: 20
        type: integer
      page:
        example: 1
        type: integer
      products:
        items:
          $ref: '#/definitions/models.Product'
        type: array
      total:
        example: 100
        type: integer
    type: object
  models.RefreshResponse:
    properties:
      access_token:
        example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
        type: string
      message:
        example: token refreshed successfully
        type: string
      user:
        $ref: '#/definitions/models.UserInfo'
    type: object
  models.RegisterRequest:
    properties:
      email:
        example: user@example.com
        type: string
      full_name:
        example: John Doe
        type: string
      password:
        example: password123
        minLength: 6
        type: string
      phone_number:
        example: "+1234567890"
        type: string
      username:
        example: johndoe
        maxLength: 50
        minLength: 3
        type: string
    required:
    - email
    - full_name
    - password
    - username
    type: object
  models.RegisterResponse:
    properties:
      access_token:
        example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
        type: string
      message:
        example: user registered successfully
        type: string
      user:
        $ref: '#/definitions/models.UserInfo'
    type: object
  models.SearchResponse:
    properties:
      limit:
        example: 20
        type: integer
      page:
        example: 1
        type: integer
      products:
        items:
          $ref: '#/definitions/models.Product'
        type: array
      total:
        example: 100
        type: integer
    type: object
  models.SelectAllRequest:
    properties:
      selected:
        example: true
        type: boolean
    type: object
  models.SelectShopRequest:
    properties:
      selected:
        example: true
        type: boolean
    type: object
  models.SelectionRequest:
    properties:
      selected:
        example: true
        type: boolean
    type: object
  models.SuccessResponse:
    properties:
      data: {}
      message:
        example: Operation successful
        type: string
    type: object
  models.UpdateAddressRequest:
    properties:
      city:
        example: Ho Chi Minh City
        type: string
      country:
        example: Vietnam
        type: string
      is_default:
        example: false
        type: boolean
      postal_code:
        example: "70000"
        type: string
      state:
        example: Ho Chi Minh
        type: string
      street:
        example: 123 Main St
        type: string
    type: object
  models.UpdateCartRequest:
    properties:
      quantity:
        example: 2
        type: integer
    type: object
  models.UpdateCategoryRequest:
    properties:
      description:
        example: Updated description
        type: string
      name:
        example: Electronics Updated
        type: string
      parent_id:
        example: 0
        type: integer
      slug:
        example: electronics-updated
        type: string
    type: object
  models.UpdateProductRequest:
    properties:
      category_id:
        example: 1
        type: integer
      description:
        example: Updated description
        type: string
      images:
        items:
          type: string
        type: array
      is_active:
        example: true
        type: boolean
      name:
        example: iPhone 15 Pro Updated
        type: string
      price:
        example: 1099.99
        type: number
      status:
        example: ACTIVE
        type: string
      stock:
        example: 60
        type: integer
    type: object
  models.UpdateProfileRequest:
    properties:
      full_name:
        example: John Doe Updated
        type: string
      phone_number:
        example: "+1234567890"
        type: string
    type: object
  models.UserInfo:
    properties:
      email:
        example: user@example.com
        type: string
      full_name:
        example: John Doe
        type: string
      id:
        example: 1
        type: integer
      phone_number:
        example: "+1234567890"
        type: string
      role:
        example: user
        type: string
      status:
        example: ACTIVE
        type: string
      username:
        example: johndoe
        type: string
    type: object
host: localhost:8000
info:
  contact:
    email: support@example.com
    name: API Support
  description: Comprehensive API documentation for the Ecommerce Platform. All endpoints
    are accessed through the API Gateway which acts as a reverse proxy to backend
    microservices (Identity Service, Product Service, Order Service, etc.). The Gateway
    automatically forwards cookies, headers, and user context to backend services.
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  termsOfService: http://swagger.io/terms/
  title: Ecommerce Platform API Gateway
  version: "2.0"
paths:
  /addresses:
    get:
      consumes:
      - application/json
      description: Get all addresses for the authenticated user
      produces:
      - application/json
      responses:
        "200":
          description: List of addresses
          schema:
            items:
              $ref: '#/definitions/models.AddressInfo'
            type: array
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Get all addresses
      tags:
      - Addresses
    post:
      consumes:
      - application/json
      description: Create a new address for the authenticated user
      parameters:
      - description: Create Address Request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.CreateAddressRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Address created successfully
          schema:
            $ref: '#/definitions/models.AddressInfo'
        "400":
          description: Bad request
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Create a new address
      tags:
      - Addresses
  /addresses/{id}:
    delete:
      consumes:
      - application/json
      description: Delete an address for the authenticated user
      parameters:
      - description: Address ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Address deleted successfully
          schema:
            $ref: '#/definitions/models.SuccessResponse'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "404":
          description: Address not found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Delete an address
      tags:
      - Addresses
    get:
      consumes:
      - application/json
      description: Get a specific address by ID for the authenticated user
      parameters:
      - description: Address ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Address details
          schema:
            $ref: '#/definitions/models.AddressInfo'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "404":
          description: Address not found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Get address by ID
      tags:
      - Addresses
    put:
      consumes:
      - application/json
      description: Update an existing address for the authenticated user
      parameters:
      - description: Address ID
        in: path
        name: id
        required: true
        type: integer
      - description: Update Address Request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.UpdateAddressRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Address updated successfully
          schema:
            $ref: '#/definitions/models.AddressInfo'
        "400":
          description: Bad request
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "404":
          description: Address not found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Update an address
      tags:
      - Addresses
  /addresses/{id}/default:
    put:
      consumes:
      - application/json
      description: Set an address as the default address for the authenticated user
      parameters:
      - description: Address ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Address set as default
          schema:
            $ref: '#/definitions/models.AddressInfo'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "404":
          description: Address not found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Set default address
      tags:
      - Addresses
  /api/gateway/health:
    get:
      description: Returns the health status of the API Gateway and all registered
        microservices
      produces:
      - application/json
      responses:
        "200":
          description: Gateway and services are healthy
          schema:
            additionalProperties: true
            type: object
        "503":
          description: One or more services are unhealthy
          schema:
            additionalProperties: true
            type: object
      summary: Health check
      tags:
      - Gateway
  /api/v1/{path}:
    delete:
      consumes:
      - application/json
      description: Routes requests to appropriate backend microservices (Identity
        Service, Product Service, etc.)
      parameters:
      - description: Bearer token for protected routes
        in: header
        name: Authorization
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Response from backend service
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad request
          schema:
            additionalProperties: true
            type: object
        "401":
          description: Unauthorized
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal server error
          schema:
            additionalProperties: true
            type: object
      summary: Proxy request to microservice
      tags:
      - Gateway
    get:
      consumes:
      - application/json
      description: Routes requests to appropriate backend microservices (Identity
        Service, Product Service, etc.)
      parameters:
      - description: Bearer token for protected routes
        in: header
        name: Authorization
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Response from backend service
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad request
          schema:
            additionalProperties: true
            type: object
        "401":
          description: Unauthorized
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal server error
          schema:
            additionalProperties: true
            type: object
      summary: Proxy request to microservice
      tags:
      - Gateway
    patch:
      consumes:
      - application/json
      description: Routes requests to appropriate backend microservices (Identity
        Service, Product Service, etc.)
      parameters:
      - description: Bearer token for protected routes
        in: header
        name: Authorization
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Response from backend service
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad request
          schema:
            additionalProperties: true
            type: object
        "401":
          description: Unauthorized
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal server error
          schema:
            additionalProperties: true
            type: object
      summary: Proxy request to microservice
      tags:
      - Gateway
    post:
      consumes:
      - application/json
      description: Routes requests to appropriate backend microservices (Identity
        Service, Product Service, etc.)
      parameters:
      - description: Bearer token for protected routes
        in: header
        name: Authorization
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Response from backend service
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad request
          schema:
            additionalProperties: true
            type: object
        "401":
          description: Unauthorized
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal server error
          schema:
            additionalProperties: true
            type: object
      summary: Proxy request to microservice
      tags:
      - Gateway
    put:
      consumes:
      - application/json
      description: Routes requests to appropriate backend microservices (Identity
        Service, Product Service, etc.)
      parameters:
      - description: Bearer token for protected routes
        in: header
        name: Authorization
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Response from backend service
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad request
          schema:
            additionalProperties: true
            type: object
        "401":
          description: Unauthorized
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal server error
          schema:
            additionalProperties: true
            type: object
      summary: Proxy request to microservice
      tags:
      - Gateway
  /auth/login:
    post:
      consumes:
      - application/json
      description: Authenticate user with email and password. Returns access_token
        in response body (15 min, store in memory) and sets refresh_token as HttpOnly
        cookie (7 days). The Gateway forwards credentials to Identity Service which
        validates them and returns tokens. Frontend should store access_token in memory
        (NOT localStorage) and send it via Authorization header. refresh_token cookie
        is sent automatically by browser.
      parameters:
      - description: Login credentials (email and password)
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.LoginRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Login successful. Response contains access_token (15min, store
            in memory) and user info. refresh_token sent as HttpOnly cookie (7 days)
          headers:
            Set-Cookie:
              description: refresh_token=<jwt>; Path=/; Max-Age=604800; HttpOnly
              type: string
          schema:
            $ref: '#/definitions/models.LoginResponse'
        "400":
          description: Bad request - invalid input format
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "401":
          description: Invalid credentials - wrong email or password
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Login user
      tags:
      - Authentication
  /auth/logout:
    post:
      consumes:
      - application/json
      description: Revoke all refresh tokens for the authenticated user and clear
        refresh_token cookie. Requires valid access_token in Authorization header
        (Bearer token). The Gateway forwards the request to Identity Service which
        revokes all refresh tokens from the database. Frontend should discard the
        access_token from memory.
      parameters:
      - default: Bearer <your_access_token>
        description: Bearer <access_token>
        in: header
        name: Authorization
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Logout successful. All refresh tokens revoked and refresh_token
            cookie cleared. Frontend should discard access_token from memory
          headers:
            Set-Cookie:
              description: refresh_token=; Path=/; Max-Age=-1; HttpOnly (cleared)
              type: string
          schema:
            additionalProperties: true
            type: object
        "401":
          description: Unauthorized - missing or invalid access token
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "500":
          description: Internal server error - failed to revoke tokens
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Logout user
      tags:
      - Authentication
  /auth/refresh:
    post:
      consumes:
      - application/json
      description: Use refresh_token from HttpOnly cookie to obtain a new access_token.
        The Gateway automatically forwards the cookie to Identity Service which validates
        the refresh token and issues a new access_token (15 min) in response body.
        The refresh_token cookie remains unchanged. Frontend should update the access_token
        in memory.
      parameters:
      - default: refresh_token=<your_refresh_token>
        description: refresh_token cookie (HttpOnly, automatically sent by browser)
        in: header
        name: Cookie
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Token refreshed successfully. New access_token returned in
            body (store in memory). refresh_token cookie unchanged
          schema:
            $ref: '#/definitions/models.RefreshResponse'
        "401":
          description: Invalid, expired, or revoked refresh token
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Refresh access token
      tags:
      - Authentication
  /auth/register:
    post:
      consumes:
      - application/json
      description: Register a new user account. Returns access_token in response body
        (store in memory) and sets refresh_token as HttpOnly cookie (7 days). The
        Gateway forwards the request to Identity Service which validates data, creates
        user, and returns tokens. Frontend should store access_token in memory (NOT
        localStorage) for security.
      parameters:
      - description: Registration data (email, password, username, full_name)
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.RegisterRequest'
      produces:
      - application/json
      responses:
        "201":
          description: User registered successfully. Response contains access_token
            (15min, store in memory) and user info. refresh_token sent as HttpOnly
            cookie (7 days)
          headers:
            Set-Cookie:
              description: refresh_token=<jwt>; Path=/; Max-Age=604800; HttpOnly
              type: string
          schema:
            $ref: '#/definitions/models.RegisterResponse'
        "400":
          description: Bad request - invalid input format
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "409":
          description: User already exists - email or username taken
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Register a new user
      tags:
      - Authentication
  /cart:
    delete:
      description: Xóa tất cả sản phẩm trong giỏ hàng của người dùng
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.SuccessResponse'
        "401":
          description: Chưa xác thực
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "500":
          description: Lỗi hệ thống
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Xóa toàn bộ giỏ hàng
      tags:
      - Cart
    get:
      consumes:
      - application/json
      description: Lấy danh sách sản phẩm trong giỏ hàng của người dùng hiện tại (yêu
        cầu đăng nhập)
      produces:
      - application/json
      responses:
        "200":
          description: Thông tin giỏ hàng
          schema:
            $ref: '#/definitions/models.CartResponse'
        "401":
          description: Chưa xác thực
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "500":
          description: Lỗi hệ thống
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      - CookieAuth: []
      summary: Lấy nội dung giỏ hàng
      tags:
      - Cart
  /cart/items:
    post:
      consumes:
      - application/json
      description: Thêm một sản phẩm cụ thể (SKU) vào giỏ hàng với số lượng nhất định
      parameters:
      - description: Thông tin sản phẩm thêm vào
        in: body
        name: item
        required: true
        schema:
          $ref: '#/definitions/models.AddToCartRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/models.CartResponse'
        "400":
          description: Dữ liệu không hợp lệ hoặc thiếu hàng
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Thêm sản phẩm vào giỏ
      tags:
      - Cart
  /cart/items/{product_item_id}:
    delete:
      description: Xóa một sản phẩm cụ thể ra khỏi giỏ hàng
      parameters:
      - description: ID của SKU sản phẩm
        in: path
        name: product_item_id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.SuccessResponse'
        "400":
          description: Tham số không hợp lệ
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "401":
          description: Chưa xác thực
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "404":
          description: Không tìm thấy sản phẩm
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "500":
          description: Lỗi hệ thống
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Xóa sản phẩm khỏi giỏ hàng
      tags:
      - Cart
    put:
      consumes:
      - application/json
      description: Thay đổi số lượng của một SKU cụ thể trong giỏ hàng
      parameters:
      - description: ID của SKU sản phẩm
        in: path
        name: product_item_id
        required: true
        type: integer
      - description: Số lượng mới
        in: body
        name: quantity
        required: true
        schema:
          $ref: '#/definitions/models.UpdateCartRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.CartResponse'
      security:
      - BearerAuth: []
      summary: Cập nhật số lượng sản phẩm
      tags:
      - Cart
  /cart/items/{product_item_id}/selection:
    patch:
      consumes:
      - application/json
      description: Set the selection state (selected/deselected) for a cart item
      parameters:
      - description: ID của SKU sản phẩm
        in: path
        name: product_item_id
        required: true
        type: integer
      - description: Trạng thái chọn
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.SelectionRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.SuccessResponse'
        "400":
          description: Dữ liệu không hợp lệ
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "401":
          description: Chưa xác thực
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "404":
          description: Không tìm thấy sản phẩm
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "500":
          description: Lỗi hệ thống
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Set item selection
      tags:
      - Cart
  /cart/items/{product_item_id}/toggle:
    post:
      consumes:
      - application/json
      description: Thay đổi trạng thái được chọn (tick) của một sản phẩm để chuẩn
        bị thanh toán
      parameters:
      - description: ID của SKU sản phẩm
        in: path
        name: product_item_id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.CartResponse'
      security:
      - BearerAuth: []
      summary: Bật/Tắt chọn sản phẩm
      tags:
      - Cart
  /cart/select_all:
    post:
      consumes:
      - application/json
      description: Chọn hoặc bỏ chọn toàn bộ sản phẩm có trong giỏ hàng
      parameters:
      - description: Trạng thái chọn (true/false)
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.SelectAllRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.SuccessResponse'
      security:
      - BearerAuth: []
      summary: Chọn/Bỏ chọn tất cả
      tags:
      - Cart
  /cart/selected:
    delete:
      description: Xóa tất cả sản phẩm đang được tick chọn ra khỏi giỏ hàng
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.SuccessResponse'
      security:
      - BearerAuth: []
      summary: Xóa các mục đã chọn
      tags:
      - Cart
  /cart/selection:
    patch:
      consumes:
      - application/json
      description: Set selection state for all items in the cart
      parameters:
      - description: Trạng thái chọn
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.SelectAllRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.SuccessResponse'
        "400":
          description: Dữ liệu không hợp lệ
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "401":
          description: Chưa xác thực
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "500":
          description: Lỗi hệ thống
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Set selection for all items
      tags:
      - Cart
  /cart/shops/{shop_id}/select:
    post:
      consumes:
      - application/json
      description: Chọn hoặc bỏ chọn tất cả sản phẩm thuộc về một cửa hàng cụ thể
      parameters:
      - description: ID của cửa hàng
        in: path
        name: shop_id
        required: true
        type: integer
      - description: Trạng thái chọn
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.SelectAllRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.SuccessResponse'
      security:
      - BearerAuth: []
      summary: Chọn sản phẩm theo Shop
      tags:
      - Cart
  /cart/shops/{shop_id}/selection:
    patch:
      consumes:
      - application/json
      description: Set selection state for all items from a shop
      parameters:
      - description: ID của cửa hàng
        in: path
        name: shop_id
        required: true
        type: integer
      - description: Trạng thái chọn
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.SelectShopRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.SuccessResponse'
        "400":
          description: Dữ liệu không hợp lệ
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "401":
          description: Chưa xác thực
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "500":
          description: Lỗi hệ thống
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Set selection for shop items
      tags:
      - Cart
  /cart/validate:
    post:
      description: Kiểm tra tồn kho và trạng thái của các sản phẩm đã chọn trước khi
        chuyển sang bước thanh toán
      produces:
      - application/json
      responses:
        "200":
          description: Kết quả kiểm tra
          schema:
            $ref: '#/definitions/models.CartValidationResponse'
        "400":
          description: Sản phẩm không hợp lệ hoặc hết hàng
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Kiểm tra giỏ hàng trước thanh toán
      tags:
      - Cart
  /categories:
    get:
      consumes:
      - application/json
      description: Get a list of all categories
      produces:
      - application/json
      responses:
        "200":
          description: List of categories
          schema:
            items:
              $ref: '#/definitions/models.Category'
            type: array
      summary: Get all categories
      tags:
      - Categories
    post:
      consumes:
      - application/json
      description: Create a new category
      parameters:
      - description: Create Category Request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.CreateCategoryRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Category created successfully
          schema:
            $ref: '#/definitions/models.SuccessResponse'
        "400":
          description: Bad request
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Create a new category
      tags:
      - Categories
  /categories/{id}:
    delete:
      consumes:
      - application/json
      description: Delete a category
      parameters:
      - description: Category ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Category deleted successfully
          schema:
            $ref: '#/definitions/models.SuccessResponse'
        "404":
          description: Category not found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Delete a category
      tags:
      - Categories
    get:
      consumes:
      - application/json
      description: Get detailed information about a specific category
      parameters:
      - description: Category ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Category details
          schema:
            $ref: '#/definitions/models.Category'
        "404":
          description: Category not found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Get category by ID
      tags:
      - Categories
    put:
      consumes:
      - application/json
      description: Update an existing category
      parameters:
      - description: Category ID
        in: path
        name: id
        required: true
        type: integer
      - description: Update Category Request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.UpdateCategoryRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Category updated successfully
          schema:
            $ref: '#/definitions/models.SuccessResponse'
        "400":
          description: Bad request
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "404":
          description: Category not found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Update an existing category
      tags:
      - Categories
  /categories/{id}/children:
    get:
      consumes:
      - application/json
      description: Get all child categories of a parent category
      parameters:
      - description: Parent Category ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: List of child categories
          schema:
            items:
              $ref: '#/definitions/models.Category'
            type: array
        "404":
          description: Category not found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Get child categories
      tags:
      - Categories
  /categories/{id}/products:
    get:
      consumes:
      - application/json
      description: Get all products in a specific category with pagination
      parameters:
      - description: Category ID
        in: path
        name: id
        required: true
        type: integer
      - default: 1
        description: Page number
        in: query
        name: page
        type: integer
      - default: 20
        description: Items per page
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: List of products in category
          schema:
            $ref: '#/definitions/models.ProductsResponse'
        "404":
          description: Category not found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Get products by category
      tags:
      - Categories
  /categories/slug/{slug}:
    get:
      consumes:
      - application/json
      description: Get category information by its slug
      parameters:
      - description: Category slug
        in: path
        name: slug
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Category details
          schema:
            $ref: '#/definitions/models.Category'
        "404":
          description: Category not found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Get category by slug
      tags:
      - Categories
  /health:
    get:
      description: Returns the health status of the API Gateway and all registered
        microservices
      produces:
      - application/json
      responses:
        "200":
          description: Gateway and services are healthy
          schema:
            additionalProperties: true
            type: object
        "503":
          description: One or more services are unhealthy
          schema:
            additionalProperties: true
            type: object
      summary: Health check
      tags:
      - Gateway
  /orders:
    get:
      description: Lấy danh sách đơn hàng theo user_id hoặc session_id
      parameters:
      - description: User ID
        in: query
        name: user_id
        type: integer
      - description: Session ID
        in: query
        name: session_id
        type: string
      - description: Limit
        in: query
        name: limit
        type: integer
      - description: Offset
        in: query
        name: offset
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/models.OrderResponse'
            type: array
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Lấy danh sách đơn hàng
      tags:
      - Order
    post:
      consumes:
      - application/json
      description: Tạo đơn hàng (hỗ trợ multi-shop). Hỗ trợ cả user_id (header X-User-Id)
        hoặc session_id cho guest
      parameters:
      - description: Thông tin tạo đơn
        in: body
        name: order
        required: true
        schema:
          $ref: '#/definitions/models.CreateOrderRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/models.OrderResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Tạo đơn hàng từ giỏ hàng
      tags:
      - Order
  /orders/{id}:
    get:
      parameters:
      - description: Order ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.OrderResponse'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Lấy đơn hàng theo ID
      tags:
      - Order
  /orders/number/{order_number}:
    get:
      parameters:
      - description: Order number
        in: path
        name: order_number
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.OrderResponse'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Lấy đơn hàng theo số đơn
      tags:
      - Order
  /products:
    get:
      consumes:
      - application/json
      description: Get a paginated list of products with optional filters (category,
        status)
      parameters:
      - default: 1
        description: Page number
        in: query
        name: page
        type: integer
      - default: 20
        description: Items per page
        in: query
        name: limit
        type: integer
      - description: Filter by category ID
        in: query
        name: category_id
        type: integer
      - description: Filter by status (ACTIVE, INACTIVE, DRAFT)
        in: query
        name: status
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: List of products
          schema:
            $ref: '#/definitions/models.ProductsResponse'
        "400":
          description: Bad request
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: List products with pagination and filters
      tags:
      - Products
    post:
      consumes:
      - application/json
      description: Create a new product (requires authentication)
      parameters:
      - description: Create Product Request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.CreateProductRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Product created successfully
          schema:
            $ref: '#/definitions/models.SuccessResponse'
        "400":
          description: Bad request
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Create a new product
      tags:
      - Products
  /products/{id}:
    delete:
      consumes:
      - application/json
      description: Delete a product (requires authentication)
      parameters:
      - description: Product ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Product deleted successfully
          schema:
            $ref: '#/definitions/models.SuccessResponse'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "404":
          description: Product not found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Delete a product
      tags:
      - Products
    get:
      consumes:
      - application/json
      description: Get detailed information about a specific product
      parameters:
      - description: Product ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Product details
          schema:
            $ref: '#/definitions/models.Product'
        "404":
          description: Product not found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Get product by ID
      tags:
      - Products
    put:
      consumes:
      - application/json
      description: Update an existing product (requires authentication)
      parameters:
      - description: Product ID
        in: path
        name: id
        required: true
        type: integer
      - description: Update Product Request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.UpdateProductRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Product updated successfully
          schema:
            $ref: '#/definitions/models.SuccessResponse'
        "400":
          description: Bad request
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "404":
          description: Product not found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Update an existing product
      tags:
      - Products
  /products/{id}/inventory:
    patch:
      consumes:
      - application/json
      description: Update product stock quantity (requires authentication)
      parameters:
      - description: Product ID
        in: path
        name: id
        required: true
        type: integer
      - description: Stock update
        in: body
        name: stock
        required: true
        schema:
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: Inventory updated successfully
          schema:
            $ref: '#/definitions/models.SuccessResponse'
        "400":
          description: Bad request
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "404":
          description: Product not found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Update product inventory
      tags:
      - Products
  /products/{id}/items:
    get:
      consumes:
      - application/json
      description: Get list of all SKUs/variations for a specific product
      parameters:
      - description: Product ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: List of product items
          schema:
            type: object
        "404":
          description: Product not found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Get all product items (SKUs) for a product
      tags:
      - Product Items
    post:
      consumes:
      - application/json
      description: Create a new SKU for a product (Auth required)
      parameters:
      - description: Product ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "201":
          description: Created product item
          schema:
            type: object
        "400":
          description: Bad request
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Create new product item (SKU)
      tags:
      - Product Items
  /products/{id}/items/{item_id}:
    delete:
      consumes:
      - application/json
      description: Delete a SKU (Auth required)
      parameters:
      - description: Product ID
        in: path
        name: id
        required: true
        type: integer
      - description: Product Item ID
        in: path
        name: item_id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "204":
          description: Product item deleted successfully
        "404":
          description: Product item not found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Delete product item (SKU)
      tags:
      - Product Items
    get:
      consumes:
      - application/json
      description: Get details of a specific SKU by product ID and item ID
      parameters:
      - description: Product ID
        in: path
        name: id
        required: true
        type: integer
      - description: Product Item ID
        in: path
        name: item_id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Product item details
          schema:
            type: object
        "404":
          description: Product item not found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Get specific product item (SKU)
      tags:
      - Product Items
    put:
      consumes:
      - application/json
      description: Update an existing SKU (Auth required)
      parameters:
      - description: Product ID
        in: path
        name: id
        required: true
        type: integer
      - description: Product Item ID
        in: path
        name: item_id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Updated product item
          schema:
            type: object
        "400":
          description: Bad request
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "404":
          description: Product item not found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Update product item (SKU)
      tags:
      - Product Items
  /products/{id}/variations:
    get:
      consumes:
      - application/json
      description: Get all variations (Color, Size, etc.) with their options for a
        product
      parameters:
      - description: Product ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: List of variations with options
          schema:
            type: object
        "404":
          description: Product not found
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Get product variations with options
      tags:
      - Product Variations
  /products/search:
    get:
      consumes:
      - application/json
      description: Search products by query string and optional category
      parameters:
      - description: Search query
        in: query
        name: q
        required: true
        type: string
      - description: Filter by category slug
        in: query
        name: category
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: List of matching products
          schema:
            items:
              $ref: '#/definitions/models.Product'
            type: array
        "400":
          description: Bad request
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Search products
      tags:
      - Products
  /search:
    get:
      description: Search products by keyword with filters (category, price range,
        status) and sort options. Uses Elasticsearch for full-text search. This service
        indexes products from Kafka events and provides fast search capabilities.
      parameters:
      - description: Search keyword (searches in product name and description using
          Elasticsearch full-text search)
        in: query
        name: q
        type: string
      - description: Filter by category ID
        in: query
        name: category_id
        type: integer
      - description: Minimum price
        in: query
        name: min_price
        type: number
      - description: Maximum price
        in: query
        name: max_price
        type: number
      - description: Filter by status (ACTIVE, INACTIVE)
        in: query
        name: status
        type: string
      - default: created_at
        description: Sort field (price, name, created_at)
        in: query
        name: sort_field
        type: string
      - default: desc
        description: Sort order (asc, desc)
        in: query
        name: sort_order
        type: string
      - default: 1
        description: Page number
        in: query
        name: page
        type: integer
      - default: 20
        description: Items per page
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Search results with products and pagination
          schema:
            $ref: '#/definitions/models.SearchResponse'
        "400":
          description: Invalid request parameters
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "500":
          description: Internal server error
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      summary: Search products
      tags:
      - Search
  /users/password:
    put:
      consumes:
      - application/json
      description: Change the authenticated user's password
      parameters:
      - description: Change Password Request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.ChangePasswordRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Password changed successfully
          schema:
            $ref: '#/definitions/models.SuccessResponse'
        "400":
          description: Bad request
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Change user password
      tags:
      - Users
  /users/profile:
    get:
      consumes:
      - application/json
      description: Get the authenticated user's profile information
      produces:
      - application/json
      responses:
        "200":
          description: User profile
          schema:
            $ref: '#/definitions/models.UserInfo'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Get user profile
      tags:
      - Users
    put:
      consumes:
      - application/json
      description: Update the authenticated user's profile information
      parameters:
      - description: Update Profile Request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.UpdateProfileRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Profile updated successfully
          schema:
            $ref: '#/definitions/models.UserInfo'
        "400":
          description: Bad request
          schema:
            $ref: '#/definitions/models.ErrorResponse'
        "401":
          description: Unauthorized
          schema:
            $ref: '#/definitions/models.ErrorResponse'
      security:
      - BearerAuth: []
      summary: Update user profile
      tags:
      - Users
securityDefinitions:
  BearerAuth:
    description: 'Type "Bearer" followed by a space and JWT access token. Example:
      "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."'
    in: header
    name: Authorization
    type: apiKey
  CookieAuth:
    description: HttpOnly cookie containing JWT access token. Automatically set after
      login/register. Used for authentication in browser-based applications.
    in: cookie
    name: access_token
    type: apiKey
swagger: "2.0"
